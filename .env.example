# Connection for prisma
DATABASE_URL="postgresql://root:root@postgres:5433/nest-antitail?schema=public"

# Postgres config variables
POSTGRES_USER=root
POSTGRES_PASSWORD=root
POSTGRES_DB="nest-antitail"

# Cors config
CORS_ORIGIN="http://localhost:8000"

# JWT config
JWT_ACCESS_SECRET="jwt-access-key"
